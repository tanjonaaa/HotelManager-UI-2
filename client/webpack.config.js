// webpack.config.js
import path from "path";

export default {
  // ... other webpack configurations

  devServer: {
    allowedHosts: "all", // Allow requests from all hosts
    // other devServer configurations if needed
  },
};
